# Stacks package

Stacks SDK integration and low-level transaction mechanics.

The Day 2 executable PoC is `scripts/sponsored-transaction-poc.ts`. From the repository root, run `npm run poc` or `npm run poc:broadcast`; see [the demo guide](../../docs/DEMO.md).
